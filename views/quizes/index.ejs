<div style="text-align:center;">
<table style="margin: 0 auto;">
<% if ( quizes.length===0 ) { %>
	<tr><td>No hay preguntas que coincidan con el criterio de búsqueda indicado...</td></tr>
<% } else {
	var i; for (i=0; i<quizes.length; i++) { %>
		<tr>
			<td><a href="quizes/<%= quizes[i].id %>"><%= quizes[i].pregunta %> - <%= quizes[i].tematica %></a></td>
			<td><a href="quizes/<%= quizes[i].id %>/edit"><button>Editar</button></a></td>
			<td>
				<form method="post" action="quizes/<%= quizes[i].id %>?_method=delete">
					<button type="submit" onClick="return confirm('Borrar: <%= quizes[i].pregunta %>');">Borrar</button>
				</form>
			</td>
		</tr>
	<% }} %>
</table>
<a href="/quizes/new"><button type="button">Crear pregunta</button></a>
</div>

<br>

<form method="get" action="/quizes">
  Buscar preguntas con el siguiente contenido:<br>
  <input type="text" name="search" value="Texto a buscar" >
  <input type="submit" value="Enviar" >
</form> 